# DP_TextFile_Sorter — семантический сортировщик Markdown-заметок Obsidian

Программа раскладывает копии `.md`-заметок по **уже существующей** структуре каталогов
хранилища. Структура папок пользователя — это его система категорий; программа не
придумывает новые категории, а учится понимать имеющиеся и распределяет заметки по ним.

```
Существующая структура каталогов  →  система категорий
                                  ↓
                        анализ Markdown-файла
                                  ↓
                       семантическое сравнение
                                  ↓
                    наиболее подходящая категория
                                  ↓
        Sorted_md_files/<категория>/<файл>.md   (копия, оригинал не тронут)
```

**Приоритеты:** сохранность исходных файлов → корректность классификации →
предсказуемость → проверяемость → повторяемость → расширяемость → производительность.

---

## Содержание

- [Быстрый старт](#быстрый-старт)
- [Установка и зависимости](#установка-и-зависимости)
- [Архитектура проекта](#архитектура-проекта)
- [Алгоритм классификации](#алгоритм-классификации)
- [Пример структуры INBOX](#пример-структуры-inbox)
- [Пример результата сортировки](#пример-результата-сортировки)
- [Пример `--dry-run`](#пример---dry-run)
- [Пример `--verbose`](#пример---verbose)
- [Параметры командной строки](#параметры-командной-строки)
- [Конфигурация](#конфигурация)
- [Гарантии безопасности](#гарантии-безопасности)
- [Повторные запуски](#повторные-запуски)
- [Замена алгоритма классификации](#замена-алгоритма-классификации)
- [Производительность](#производительность)
- [Тестирование](#тестирование)
- [Ограничения и дальнейшее развитие](#ограничения-и-дальнейшее-развитие)

---

## Быстрый старт

Программа работает с двумя каталогами:

| Каталог | Что это | Флаг |
|---|---|---|
| **inbox** | несортированные заметки — то, что нужно разложить | `--inbox` |
| **хранилище** | откуда берётся система категорий | `--vault` |

Если категории лежат прямо в том же каталоге, что и заметки, второй флаг не нужен:

```bash
cd ~/Obsidian/INBOX
python /путь/к/sorter.py --dry-run
python /путь/к/sorter.py
```

### Структура категорий на уровень выше

Типичное хранилище Obsidian устроено так: категории в корне, несортированное —
в отдельной папке внутри.

```
MAIN_OBSIDIAN_PC/        ← хранилище: отсюда берётся система категорий
├── INBOX/               ← inbox: что нужно разложить
├── Programming/
├── Games/
└── Linux/
```

Один раз укажите пути и сохраните их:

```bash
cd /sdcard/MAIN_OBSIDIAN_PC/INBOX
python /путь/к/sorter.py --vault .. --save-config --dry-run
```

Дальше флаги больше не нужны:

```bash
python /путь/к/sorter.py --dry-run    # предпросмотр
python /путь/к/sorter.py              # сортировка
```

Пути сохраняются в `<inbox>/md_sorter.toml` и читаются при каждом запуске.
Чтобы настройки работали из любого каталога, сохраните их глобально:

```bash
python /путь/к/sorter.py --vault .. --save-config ~/.config/md_sorter/config.toml
```

**Важно:** заметки, уже разложенные по категориям хранилища, не сортируются и не
копируются — они служат обучающим материалом. Раскладывается только содержимое
inbox. Исходные `.md` не изменяются, копии появляются в `<inbox>/Sorted_md_files/`
(каталог задаётся флагом `--output`).

---

## Установка и зависимости

**Базовая версия не требует ничего, кроме Python 3.11+.** Ни одной внешней библиотеки.
Это сделано намеренно: программа должна запускаться в том числе в Termux на Android.

```bash
python --version   # нужен 3.11 или новее (используется tomllib)
python sorter.py --help
```

| Файл | Что ставит | Когда нужен |
|---|---|---|
| `requirements.txt` | ничего | базовый режим `--classifier lexical` |
| `requirements-optional.txt` | `scikit-learn`, `sentence-transformers`, `anthropic`, `PyYAML` | альтернативные классификаторы |
| `requirements-dev.txt` | `pytest` | запуск тестов |

Опциональные зависимости ставятся по одной, по мере необходимости:

```bash
pip install scikit-learn          # --classifier tfidf
pip install sentence-transformers # --classifier embeddings, --classifier hybrid
pip install anthropic             # --classifier llm  (нужен ANTHROPIC_API_KEY)
pip install PyYAML                # более строгий разбор frontmatter (не обязателен)
```

Проверено на Linux, Windows и Termux/Android. Абсолютных путей в коде нет,
все пути строятся через `pathlib`.

### Termux / Android

```bash
pkg update && pkg upgrade
pkg install python
termux-setup-storage          # разрешить доступ к /sdcard

cd /sdcard/MAIN_OBSIDIAN_PC
python /storage/emulated/0/CODE/PYTHON/DP_Obsdian_Sorter/sorter.py --dry-run
```

Хранилище на `/sdcard` смонтировано через FUSE: там запрещены `chmod` и `utime`,
не работают символические ссылки, а имена файлов подчиняются ограничениям FAT.
Программа это учитывает — содержимое копируется, а перенос прав и времени
изменения выполняется по возможности и не считается ошибкой.

Если `python` отвечает `Permission denied`, интерпретатор не установлен или
найден на разделе, смонтированном с `noexec`. Проверить:

```bash
command -v python && ls -l "$(command -v python)" && echo "$PATH"
```

Путь должен указывать в `/data/data/com.termux/files/usr/bin`, а не в `/sdcard`.

---

## Архитектура проекта

```
DP_TextFile_Sorter/
├── sorter.py                  # точка входа: python sorter.py
├── md_sorter/
│   ├── __main__.py            # альтернативный запуск: python -m md_sorter
│   ├── cli.py                 # разбор аргументов и оркестрация конвейера
│   ├── config.py              # Config, ScoringWeights, чтение md_sorter.toml
│   ├── models.py              # NoteRecord, ParsedNote, Category, Decision, Stats
│   ├── logging_setup.py       # формат [INFO]/[WARNING]/[ERROR], цвет только для TTY
│   ├── scanner.py             # рекурсивный обход, отсев, безопасное чтение
│   ├── markdown_parser.py     # frontmatter, заголовки, теги, wiki-links, код
│   ├── text.py                # токенизация, стеммер, IDF, косинусная близость
│   ├── lexicon.py             # словарь терминов категорий (RU + EN)
│   ├── structure.py           # категории из ROOT + зеркалирование структуры
│   ├── decision.py            # порог, неоднозначность, стратегии запаса
│   ├── file_manager.py        # копирование, конфликты имён, манифест
│   ├── report.py              # ход работы, объяснения, статистика
│   └── classifier/
│       ├── base.py            # абстрактный интерфейс Classifier
│       ├── lexical.py         # ★ алгоритм по умолчанию, без зависимостей
│       ├── tfidf.py           # scikit-learn
│       ├── embeddings.py      # sentence-transformers + кэш
│       ├── llm.py             # Claude API как арбитр коротких списков
│       ├── hybrid.py          # взвешенная комбинация
│       └── __init__.py        # реестр классификаторов
├── tests/                     # pytest, полностью офлайн
├── tools/make_demo_inbox.py   # генератор демонстрационного хранилища
├── md_sorter.example.toml     # пример конфигурации
└── requirements*.txt
```

Конвейер строго однонаправленный:

```
cli → scanner → markdown_parser → structure → classifier → decision → file_manager → report
```

---

## Алгоритм классификации

### 1. Что извлекается из заметки

| Признак | Вес | Примечание |
|---|---|---|
| имя файла | 3.0 | «сильный» признак |
| теги frontmatter и инлайн-теги `#python`, `#linux/arch` | 3.0 | «сильный» |
| `aliases`, `category`, `type` из frontmatter | 3.0 | «сильный» |
| заголовок H1 | 2.5 | «сильный» |
| заголовки H2–H3 | 2.0 | «сильный» |
| язык кодового блока (` ```python `) | 2.5 | «сильный» |
| `[[wiki-links]]` | 1.5 | «сильный» |
| содержимое кодовых блоков (импорты, команды) | 1.5 | |
| исходный каталог заметки | 1.0 | плюс отдельный бонус |
| основной текст и списки | 1.0 | |

«Сильные» признаки собираются в отдельный вектор — по нему делается второй проход
(см. пункт 3), чтобы длинная проза не размывала однозначный сигнал вроде `tags: [minecraft]`.

### 2. Что известно о категории

Профиль каталога складывается из:

1. **имени** каталога (вес 3.0) — `Dev-Tools` → `dev`, `tools`;
2. **полного пути** (вес 2.0) — `Linux/Termux` даёт и `linux`, и `termux`, поэтому
   каталог читается как «Linux → Termux», а не просто «Termux»;
3. **встроенного словаря** (`lexicon.py`) — `arch` → `pacman`, `aur`, `yay`, …;
   термины в списке идут от самого характерного, вес убывает как `1/√i`, а масса
   всего блока растёт как `√N`, чтобы длинный список синонимов не размывал вектор;
4. **пользовательских алиасов** из `md_sorter.toml` — способ подстроить программу
   под своё хранилище без правки кода;
5. **заметок, которые уже лежат в этом каталоге** — лучшее описание категории.
   Вклад делится на `√N` и ограничен сверху массой собственных признаков категории;
   при оценке заметки её собственный вклад из профиля вычитается, иначе каталог
   с одной заметкой превратился бы в детектор её копий.

### 3. Три независимых источника сигнала

Встроенный словарь не знает ваших личных категорий — он не подскажет, что такое
`Хобби/Мотоциклы`. Поэтому главный источник смысла не словарь, а **само хранилище**.

| Источник | Что это | Когда решает |
|---|---|---|
| **Профиль категории** | косинусная близость к имени, пути, словарю и алиасам | категория названа прямо в заметке |
| **Соседи (k-NN)** | голос ближайших уже разложенных заметок | категория личная, но в ней что-то лежит |
| **Ссылки** | категории целей `[[wiki-links]]` | заметка ссылается на уже разложенную |

Сигналы объединяются по правилу «шумного ИЛИ»: `1 − ∏(1 − сигнал)`. Любого одного
достаточно, чтобы поднять оценку, а согласие нескольких усиливает её сильнее, чем
каждый по отдельности.

Голос соседа учитывается полностью только при совпадении нескольких терминов:
одно случайное общее слово вроде «проверил» не должно перевешивать три точных
термина по теме.

Два приёма нормировки, без которых короткие заметки классифицировались плохо:

- **сублинейный вес** `1 + ln(w)`. Слово из имени файла получает вес 3.0, вместе
  с заголовком и текстом — 6.5, и после умножения на максимальный IDF занимает
  почти всю норму вектора, глуша содержательные термины;
- **ограниченный словарь.** Токен, которого нет ни в одной категории и ни в одной
  разложенной заметке, не может повлиять на выбор, но при нормировке съедает долю
  нормы. Такие токены отбрасываются до нормировки. В замерах слово из заголовка
  занимало 95 % нормы вектора, после исправления косинус с нужной категорией
  вырос вчетверо.

Голос соседей демпфируется числом заметок в категории: одного соседа мало для
вывода, иначе каталог с единственной заметкой притягивал бы всё на неё похожее.

### 4. Как считается итоговая оценка

```
similarity = max( cosine(вектор_всей_заметки,  вектор_категории),
                  cosine(вектор_сильных_признаков, вектор_категории) )

base       = 1 − (1 − similarity) · (1 − голос_соседей) · (1 − голос_ссылок)

score      = base
           + 0.35 · base(родительской категории)            # поддержка родителя
           × 1.15  если имя категории есть в сильных признаках
           + 0.10  если заметка уже лежала в этом каталоге
           × (1 + 0.06 · (глубина − 1))                     # бонус за конкретность
```

Векторы разреженные, взвешенные по IDF (считается по всему хранилищу);
сравнение со всеми категориями выполняется за один проход по инвертированному индексу.

**Правило потомка.** Если лидер является предком другого сильного кандидата и разрыв
между ними не больше 10 %, побеждает потомок. Поэтому при наличии `Linux`, `Linux/Arch`
и `Linux/Termux` заметка про Arch попадает именно в `Linux/Arch`.

### 5. Самообучение

В «холодном» хранилище, где ещё ничего не разложено, категории описаны только
своими названиями. Первый проход опознаёт то, что названо прямо
(«Мотоцикл: замена масла» → `Хобби/Мотоциклы`), эти заметки становятся примерами,
и второй проход узнаёт по ним всё остальное — заметку «Смазал цепь, проверил
уровень масла», где слова «мотоцикл» нет вовсе.

Обучение идёт **только на уверенных решениях** (`self_training_min_score`), иначе
принятая «за неимением лучшего» заметка стала бы примером категории и потянула бы
за собой похожие ошибки. Отключается через `--self-training-rounds 0`.

### 6. Когда решение принимается

Есть два независимых основания, и достаточно любого:

1. **абсолютное** — оценка не ниже `threshold` (по умолчанию `0.22`);
2. **относительное** — лидер оторвался от ближайшего несвязанного соперника не
   менее чем в `dominance` раз (`1.5`) и набрал хотя бы `min_evidence` (`0.05`).

Второе основание принципиально. Величина косинуса зависит от «толщины» профиля
категории: у `Programming/Python` он подкреплён словарём и набирает 0.6, а у
личной категории `Хобби/Мотоциклы` профиль — два слова, и даже очевидное
совпадение даёт 0.08. Отрыв от соперников от этого не зависит. Без этого правила
больше трети заметок уходило в `_NEEDS_REVIEW` при верно определённой категории.

Решение считается сомнительным, если:

- ни одно из двух оснований не выполнено;
- разрыв между лидером и соперником меньше `margin` (`0.06`), и лидер соперника
  не подавляет (кандидаты, связанные отношением предок/потомок, соперниками не
  считаются — их спор решает правило конкретности);
- в структуре вообще нет каталогов назначения.

Если два спорных кандидата — соседи с общим родителем, и родитель сам проходит порог,
заметка кладётся **в родителя** (`Linux` вместо спора `Arch` ↔ `Termux`).

Иначе применяется стратегия `--uncertain-strategy`:

| Стратегия | Поведение |
|---|---|
| `review` (по умолчанию) | `Sorted_md_files/_NEEDS_REVIEW/` |
| `best` | всё равно к лучшему кандидату; в `_NEEDS_REVIEW` — только заметки совсем без сигнала |
| `ancestor` | ближайший общий предок кандидатов, иначе `_NEEDS_REVIEW` |
| `root` | корень `Sorted_md_files/` |
| `skip` | не копировать, только сообщить |

`_NEEDS_REVIEW` создаётся только внутри `Sorted_md_files`, никогда в исходном
хранилище, и не участвует в классификации как категория.

---

## Пример структуры INBOX

```
INBOX/
├── Как установить пакеты в Termux.md
├── network_setup.md
├── pathlib шпаргалка.md
├── redstone.md
├── случайная мысль.md
├── Programming/
│   ├── Python/
│   │   └── декораторы.md
│   ├── C++/
│   └── JavaScript/
├── Games/
│   ├── Minecraft/
│   ├── Doom/
│   │   └── секреты.md
│   └── ULTRAKILL/
└── Linux/
    ├── Arch/
    │   └── pacman.md
    └── Termux/
```

Такое хранилище можно создать для проверки:

```bash
python tools/make_demo_inbox.py /tmp/INBOX
cd /tmp/INBOX && python /путь/к/sorter.py --dry-run
```

---

## Пример результата сортировки

```
INBOX/
├── Как установить пакеты в Termux.md      ← оригиналы на месте
├── network_setup.md
├── ...
└── Sorted_md_files/
    ├── Programming/
    │   ├── Python/
    │   │   ├── pathlib шпаргалка.md
    │   │   └── декораторы.md
    │   ├── C++/
    │   │   └── cmake заметки.md
    │   └── JavaScript/
    │       └── промисы и async.md
    ├── Games/
    │   ├── Minecraft/
    │   │   └── redstone.md
    │   ├── Doom/
    │   │   ├── doom wad.md
    │   │   └── секреты.md
    │   └── ULTRAKILL/
    │       └── ultrakill ranking.md
    ├── Linux/
    │   ├── Arch/
    │   │   ├── network_setup.md
    │   │   └── pacman.md
    │   └── Termux/
    │       └── Как установить пакеты в Termux.md
    └── _NEEDS_REVIEW/
        └── случайная мысль.md
```

Обратите внимание: в `Sorted_md_files` скопирована **структура** исходного каталога
целиком, включая пустые категории (`Games/Minecraft` останется пустым, если в неё
ничего не попало). Файлы туда переносятся только по решению классификатора.

Вывод программы:

```
[INFO] Root: /tmp/INBOX
[INFO] Searching Markdown files...
[INFO] Found: 14 files
[INFO] Building directory structure...
[INFO] Found 15 destination directories
[INFO] Classifying:
      network_setup.md
      -> Linux/Arch
      score: 0.61
[INFO] Copying:
      network_setup.md -> Sorted_md_files/Linux/Arch/network_setup.md
[INFO] Copying (конфликт имён):
      B/note.md -> Sorted_md_files/Notes/note_1.md
[WARNING] случайная мысль.md
      -> no confident category (лучшая оценка 0.14 ниже порога 0.22)
[INFO] Processed: 14   Sorted: 13   Uncertain: 1   Skipped: 0   Errors: 0
```

Коды возврата: `0` — успех, `2` — были ошибки отдельных файлов, `1` — фатальная ошибка.

---

## Пример `--dry-run`

Режим предварительного просмотра: сканирует, строит структуру в памяти, классифицирует
и показывает план — **не создавая ни одного каталога и не копируя ни одного файла**.

```bash
python sorter.py --dry-run
```

```
[INFO] Root: /tmp/INBOX
[INFO] Режим предварительного просмотра: файлы не копируются
[INFO] Searching Markdown files...
[INFO] Found: 14 files
[INFO] Found 15 destination directories
[INFO] Каталоги не создаются (--dry-run): 15 шт.
[INFO] Предполагаемые назначения:
[INFO]   note.md -> Notes
[INFO]   note.md -> Notes (как note_1.md)
[INFO]   секреты.md -> Games/Doom
[INFO]   pacman.md -> Linux/Arch
[INFO]   декораторы.md -> Programming/Python
[INFO]   cmake заметки.md -> Programming/C++
[INFO]   doom wad.md -> Games/Doom
[INFO]   network_setup.md -> Linux/Arch
[INFO]   pathlib шпаргалка.md -> Programming/Python
[INFO]   redstone.md -> Games/Minecraft
[INFO]   ultrakill ranking.md -> Games/ULTRAKILL
[INFO]   Как установить пакеты в Termux.md -> Linux/Termux
[INFO]   промисы и async.md -> Programming/JavaScript
[INFO]   случайная мысль.md -> _NEEDS_REVIEW
[WARNING] Требуют проверки: 1
[WARNING]   случайная мысль.md — лучшая оценка 0.14 ниже порога 0.22
[INFO] Processed: 14   Sorted: 13   Uncertain: 1   Skipped: 0   Errors: 0
```

---

## Пример `--verbose`

```bash
python sorter.py --dry-run --verbose
```

```
[INFO] Файл: network_setup.md
      Категория: Linux/Arch
      Почему: совпадение терминов: arch, pacman, linux, aur, yay
        1. Linux/Arch — score 0.61
           - совпадение терминов: arch, pacman, linux, aur, yay
           - имя категории «Arch» встречается в имени файла, заголовке, тегах или ссылках
           - содержание связано с родительской категорией «Linux»
           - «Linux/Arch» — более конкретная категория, чем «Linux»
        2. Linux — score 0.23
           - совпадение терминов: linux, bash, sudo, package, пакет
           - имя категории «Linux» встречается в имени файла, заголовке, тегах или ссылках
        3. Linux/Termux — score 0.14
           - совпадение терминов: linux
      Score: 0.61
```

Флаг `--debug` дополнительно печатает покомпонентную разбивку оценки:

```
           [depth_bonus=0.06, name_match=1.15, parent_support=0.0803, similarity=0.4712]
```

---

## Качество классификации

### Измерьте на своём хранилище

Заметки, которые вы уже разложили по папкам, — готовая разметка. Программа умеет
проверять себя на них: угадывает ли она ваш собственный выбор.

```bash
cd ~/Obsidian/INBOX
python /путь/к/sorter.py --evaluate
```

```
[INFO] Заметок с известной категорией: 412
[INFO] Лучший кандидат совпал с вашим выбором: 96.8%
[INFO] Принято решений (полнота): 94.4%
[INFO] Из принятых верно: 97.1%
[INFO] Категории с наибольшим числом расхождений:
[INFO]   Работа/Разное — 7
```

Режим ничего не копирует и не создаёт. По нему подбираются параметры под
конкретное хранилище.

### Что делать, если много заметок в `_NEEDS_REVIEW`

По порядку, от самого действенного:

1. **Разложите вручную по 3–5 заметок в каждую категорию.** Это самый сильный
   рычаг: хранилище становится обучающим материалом. На стенде переход от
   «ничего не разложено» к «по 6 заметок в категории» поднимает долю верно
   разложенных с 90 % до 100 %.
2. **Добавьте характерные слова категорий** в `[md_sorter.aliases]` — особенно для
   категорий с короткими или неочевидными названиями.
3. **Ослабьте правило приёма:** `--dominance 1.2`, `--min-evidence 0.02` или
   `--threshold 0.15`.
4. **Разложить всё:** `--uncertain-strategy best`. В `_NEEDS_REVIEW` останутся
   только заметки, для которых не нашлось вообще никакого сигнала.

### Замеры на стенде

`tools/benchmark.py` строит хранилище с личными категориями (которых нет во
встроенном словаре), 240 заметками разной длины и известными правильными
ответами:

```bash
python tools/benchmark.py --filed-per-topic 6
```

| Уже разложено в категории | Полнота | Верно из всех |
|---|---|---|
| 0 заметок (холодный старт) | 97.9 % | 90.8 % |
| 1 заметка | 99.2 % | 95.8 % |
| 6 заметок | 100 % | 100 % |

Для сравнения, до появления соседей, ссылок, самообучения и относительного
правила приёма: полнота 61.7 %, верно из всех 57.9 %.

## Параметры командной строки

```
python sorter.py [--inbox PATH] [--vault PATH] [--output PATH]
                 [--save-config [PATH]] [--config PATH] [--no-config]
                 [--dry-run] [--evaluate] [-v] [--debug] [-q]
                 [--log-file PATH] [--color | --no-color]
                 [--classifier {lexical,tfidf,embeddings,llm,hybrid}]
                 [--threshold FLOAT] [--margin FLOAT]
                 [--dominance FLOAT] [--min-evidence FLOAT]
                 [--knn-neighbors N] [--self-training-rounds N]
                 [--self-training-min-score FLOAT]
                 [--uncertain-strategy {review,best,ancestor,root,skip}]
                 [--top-candidates N] [--embedding-model NAME]
                 [--ignore DIR] [--include-hidden] [--follow-symlinks]
                 [--max-file-size BYTES] [--max-analysis-chars N] [--jobs N]
                 [--manifest | --no-manifest] [--move] [--allow-move]
```

Полный список с описаниями: `python sorter.py --help`.

Часто нужное:

```bash
python sorter.py --vault .. --save-config         # запомнить пути (один раз)
python sorter.py --dry-run                        # предварительный просмотр
python sorter.py --evaluate                       # измерить качество на своём хранилище
python sorter.py --verbose                        # с объяснением решений
python sorter.py --uncertain-strategy best        # разложить всё, что имеет сигнал
python sorter.py --dominance 1.2                  # мягче: меньше в _NEEDS_REVIEW
python sorter.py --threshold 0.35                 # строже: больше в _NEEDS_REVIEW
python sorter.py --uncertain-strategy ancestor    # спорное — в общего предка
python sorter.py --log-file sort.log              # лог ещё и в файл
python sorter.py --jobs 0                         # многопроцессный разбор
python sorter.py --manifest                       # вести файл состояния
```

---

## Конфигурация

Файл `md_sorter.toml` (или `.md_sorter.toml`) в корне хранилища. Полный пример —
`md_sorter.example.toml`. Все параметры необязательны, аргументы CLI имеют приоритет.

```toml
[md_sorter]
inbox = "/sdcard/MAIN_OBSIDIAN_PC/INBOX"
vault = "/sdcard/MAIN_OBSIDIAN_PC"
threshold = 0.22
uncertain_strategy = "review"
classifier = "lexical"
manifest = false

# Главный инструмент подстройки под своё хранилище: характерные слова категорий.
[md_sorter.aliases]
"Linux/Termux" = ["термукс", "телефон", "pkg", "proot"]
"Hobby/Moto" = ["мотоцикл", "цепь", "масло", "пробег"]
```

Файл ищется по порядку: `--config`, затем `<inbox>`, затем корень хранилища и
наконец `~/.config/md_sorter/config.toml`. Флаг `--save-config [PATH]` записывает
текущие пути и все параметры, отличающиеся от умолчаний; режимы одного запуска
(`--dry-run`, цвет) не сохраняются. `--no-config` полностью игнорирует файл.

---

## Гарантии безопасности

1. **Исходные файлы открываются только на чтение.** Ни одной операции записи за
   пределами `ROOT/Sorted_md_files/`.
2. **Режим по умолчанию — COPY.** `--move` заблокирован без явного `--allow-move`.
3. **Программа ничего не удаляет.** Существующий `Sorted_md_files` не очищается.
4. **Атомарная запись.** Имя резервируется через `O_EXCL`, данные пишутся во временный
   файл и переименовываются поверх — прерывание не оставит полуфайл.
5. **Запуск внутри `Sorted_md_files` запрещён** — иначе результат сортировался бы повторно.
6. **`--dry-run` не создаёт ни каталогов, ни файлов, ни манифеста.**

Проблемные файлы не роняют программу — ошибка попадает в лог, обработка продолжается:

| Случай | Поведение |
|---|---|
| символические ссылки | пропускаются, `--follow-symlinks` включает (с защитой от циклов) |
| скрытые файлы и каталоги | пропускаются, `--include-hidden` включает |
| нет прав на чтение | `[ERROR]`, счётчик ошибок, продолжаем |
| битая кодировка | анализируется с заменой символов, копируется побайтово без изменений |
| файл больше `max_file_size` | анализируется начало, `[WARNING]`, копируется целиком |
| пустой файл | классифицируется по имени и пути |
| не обычный файл (FIFO, сокет) | пропускается |
| файл исчез между сканированием и копированием | `[ERROR]`, продолжаем |

---

## Повторные запуски

Программа рассчитана на многократный запуск.

- `Sorted_md_files` **всегда** исключён из поиска — на трёх независимых рубежах
  (обрезка ветки по резолвнутому пути, пост-фильтр, проверка корня при старте).
  `Sorted_md_files/Sorted_md_files` создать невозможно.
- Если копия уже существует и **совпадает по SHA-256** — копирование пропускается.
  Поэтому второй запуск не порождает дублей и оставляет дерево неизменным.
- Если исходник изменился, старая копия **не перезаписывается**: рядом появляется
  `note_1.md`. Ничего не теряется, разбирается вручную.
- Файл состояния `Sorted_md_files/.md_sorter_manifest.json` (**только при `--manifest`**)
  позволяет пропускать неизменившиеся файлы без чтения их содержимого. Идемпотентность
  работает и без него — за счёт сверки хэшей.

---

## Замена алгоритма классификации

Все классификаторы реализуют один интерфейс `md_sorter/classifier/base.py`:

```python
class Classifier(ABC):
    def fit(self, notes, categories, notes_by_category=None) -> None: ...
    def rank(self, note, note_index=-1) -> list[Candidate]: ...
    def close(self) -> None: ...
```

Готовые варианты:

```bash
python sorter.py --classifier lexical      # по умолчанию, без зависимостей
python sorter.py --classifier tfidf        # scikit-learn
python sorter.py --classifier embeddings   # sentence-transformers, работает офлайн
python sorter.py --classifier hybrid       # 0.4 · lexical + 0.6 · embeddings
python sorter.py --classifier llm          # Claude API как арбитр
```

### Как добавить свой

Подклассы `LexicalClassifier` переопределяют **только** способ расчёта близости —
правила иерархии (поддержка родителя, бонус за конкретность, правило потомка) и
формирование объяснений наследуются:

```python
# md_sorter/classifier/my_model.py
from .lexical import LexicalClassifier


class MyClassifier(LexicalClassifier):
    name = "my-model"

    def fit(self, notes, categories, notes_by_category=None):
        super().fit(notes, categories, notes_by_category)
        self._model = load_my_model()
        self._category_vectors = self._model.encode([c.key for c in categories])

    def _base_scores(self, note, note_index, note_vector):
        """Возвращает список оценок 0..1 — по одной на каждую категорию."""
        embedding = self._model.encode(note.title or note.record.stem)
        return [float(similarity(embedding, vec)) for vec in self._category_vectors]
```

Регистрация в `md_sorter/classifier/__init__.py`:

```python
def _make_my_model(config):
    from .my_model import MyClassifier
    return MyClassifier(config)


_FACTORIES["my-model"] = _make_my_model
```

После этого `--classifier my-model` доступен, а `--help` подхватит новое имя
автоматически. Если модель принципиально другая, наследуйтесь напрямую от
`Classifier` и реализуйте `rank()` целиком.

### Классификаторы с внешними сервисами

`--classifier llm` не отправляет модели всё хранилище: лексический алгоритм отбирает
короткий список кандидатов (`llm_shortlist`, по умолчанию 10), и модель выбирает из
него. Это ограничивает объём запроса и стоимость независимо от размера хранилища.
При любой ошибке сети или отсутствии ключа программа молча возвращается к локальному
алгоритму и продолжает работу.

---

## Производительность

Замер на этой машине (Python 3.11, один процесс, `--dry-run`):

| Заметок | Время | Пик памяти |
|---|---|---|
| 5000 | 9.2 с | 73 МБ |

Что для этого сделано:

- каждый файл читается **ровно один раз**; сырой текст не хранится — в памяти живут
  компактные мешки токенов;
- обход через `os.scandir` с обрезкой веток до входа в них;
- IDF считается за один проход, векторы категорий строятся один раз;
- сравнение со всеми категориями — один проход по инвертированному индексу, а не цикл
  по каждой категории;
- анализируется не более `max_analysis_chars` символов заметки;
- поиск соседей идёт по инвертированному индексу: в него попадают только
  60 самых весомых токенов заметки, а токены, встречающиеся чаще чем в четверти
  хранилища, исключаются — они всё равно ничего не различают;
- `--jobs 0` включает многопроцессный разбор (автоматически при большом числе файлов);
  при недоступности `multiprocessing` (бывает в Termux) происходит откат в один поток;
- для `--classifier embeddings` — кэш по SHA-256 текста в
  `Sorted_md_files/.cache/embeddings.json`.

---

## Тестирование

```bash
pip install -r requirements-dev.txt
python -m pytest tests/ -q
```

114 тестов, полностью офлайн, всё во временных каталогах. Покрыты, в частности:

- токенизация RU/EN, стеммер Snowball, специальные термины (`C++`, `C#`, `.NET`);
- frontmatter — и через PyYAML, и встроенным парсером; битый YAML; `---` как разделитель;
- теги, вложенные теги, wiki-links, кодовые блоки и их языки;
- «заметка про Termux попадает в `Linux/Termux`, а не в `Linux`»;
- «заметка про Arch обходит и соседа `Termux`, и родителя `Linux`»;
- нерелевантная заметка уходит в `_NEEDS_REVIEW`;
- `--dry-run` не меняет файловую систему (сверка хэшей всего дерева);
- **идемпотентность**: два запуска подряд дают одинаковый результат без дублей;
- исходные файлы не изменяются (сверка хэшей всего дерева до и после);
- конфликты имён → `note.md`, `note_1.md`;
- `Sorted_md_files` не сканируется, вложенный `Sorted_md_files` не создаётся;
- symlink и циклы symlink, скрытые файлы, битая кодировка, BOM, огромные файлы;
- юникод и эмодзи в именах, `.md` / `.MD` / `.Md`;
- хранилище без категорий, пустое хранилище;
- чтение `md_sorter.toml`, приоритет CLI над файлом, пользовательские алиасы;
- запуск внутри `Sorted_md_files` отклоняется, `--move` без `--allow-move` отклоняется;
- обучение на хранилище: соседи опознают личную категорию, ссылки голосуют за
  категорию цели, категория с одной заметкой не притягивает всё похожее;
- самообучение поднимает «холодное» хранилище и отключается флагом;
- относительное правило приёма принимает явного лидера при низкой абсолютной
  оценке и отвергает слабый сигнал без отрыва;
- `--evaluate` ничего не меняет и печатает метрики;
- раздельная схема: категории берутся из хранилища, раскладывается только inbox,
  заметки хранилища не копируются, сам INBOX не становится категорией;
- сохранение и повторное чтение путей, приоритет флагов над сохранённым файлом;
- масштаб: 400 заметок укладываются в отведённое время.

---

## Ограничения и дальнейшее развитие

**Что алгоритм делает плохо (честно):**

- Классификатор сравнивает слова, а не смыслы напрямую. Пока в категории нет ни
  одной заметки, он опирается только на её название и встроенный словарь — для
  таких случаев есть `--classifier embeddings`.
- Качество прямо зависит от того, сколько заметок вы уже разложили сами. Это не
  недостаток, а устройство: ваша структура и есть классификатор.
- Самообучение может закрепить ошибку: если первый проход уверенно ошибся, второй
  подхватит. Поэтому обучение идёт только на решениях не слабее
  `self_training_min_score`, а число проходов по умолчанию равно одному.
- Русская морфология обрабатывается стеммером Snowball: `секреты` ≈ `секрет`, но
  `уровни` ≠ `уровень` (беглые гласные алгоритму не поддаются).

**Что напрашивается дальше:**

- интерактивный разбор `_NEEDS_REVIEW` (`--interactive`);
- отчёт в JSON/CSV для внешней оценки качества классификации;
- предложение новых категорий по кластерам «непонятых» заметок — строго как отчёт,
  без автосоздания каталогов;
- режим наблюдения за каталогом и git-hook;
- упаковка в `pyproject.toml` с консольной командой `md-sorter`.

---

## Лицензия

См. [LICENSE](LICENSE).
