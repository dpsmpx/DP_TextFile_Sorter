# Базовая версия md_sorter работает на чистой стандартной библиотеке Python 3.11+.
# Устанавливать нечего — файл существует, чтобы это было явно видно.
#
# Опциональные классификаторы:  pip install -r requirements-optional.txt
# Запуск тестов:                pip install -r requirements-dev.txt
